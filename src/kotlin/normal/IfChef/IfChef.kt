// Sometimes you need to choose between two actions:if (this) do (it) else do (something else)
fun main() {
  val isSalty = true // you can change it to val/var <anything> = <anything>
  if (isSalty) {
    println ("Oh,too salty,I need to do something")
  } else {
    println ("well,good job boy!") // else  handle
  }
}
